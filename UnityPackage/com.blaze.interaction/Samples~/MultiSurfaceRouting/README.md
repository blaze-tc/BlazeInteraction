# Multi-Surface Routing

Open `MultiSurfaceRouting.unity`. Bind `left`, `front`, and `right` to their cameras on `InteractionCameraRouter`. All surfaces share the one `InteractionManager` connection and one Bridge process.
