<UserControl x:Class="Blaze.Interaction.Bridge.Wpf.ProviderSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="25" FontWeight="SemiBold" Text="选择感应设备" />
        <TextBlock Grid.Row="1" Margin="0,8,0,18" Foreground="#667085"
                   Text="选择后将自动连接；可随时从控制界面返回并切换。" />
        <ListBox Grid.Row="2" ItemsSource="{Binding Choices}"
                 SelectedItem="{Binding SelectedChoice, Mode=TwoWay}"
                 BorderThickness="0" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,10" Padding="16" CornerRadius="8"
                            BorderBrush="#D0D5DD" BorderThickness="1">
                        <StackPanel>
                            <TextBlock FontSize="17" FontWeight="SemiBold" Text="{Binding DisplayName}" />
                            <TextBlock Margin="0,4,0,0" Foreground="#667085" Text="{Binding Category}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock Grid.Row="3" Margin="0,12,0,8" Foreground="#B42318"
                   TextWrapping="Wrap" Text="{Binding ErrorMessage}" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button MinWidth="92" Margin="0,0,10,0" Padding="14,8"
                    Command="{Binding CancelCommand}" Content="取消">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CanCancel}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button MinWidth="112" Padding="14,8" Command="{Binding ConfirmCommand}" Content="连接设备" />
        </StackPanel>
    </Grid>
</UserControl>
